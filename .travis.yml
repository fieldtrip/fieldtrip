language: java
install: source /dev/stdin <<< "$(curl -sL https://ssd.mathworks.com/supportfiles/matlab-runner/install)"
script: matlab-runner "ft_defaults; ft_test run dccnpath no maxmem 1GB maxwalltime 600"
